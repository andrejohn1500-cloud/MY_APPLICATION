<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/navy_dark"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="32dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="💛"
        android:textSize="64sp"
        android:layout_marginBottom="16dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SUPPORT US"
        android:textColor="@color/gold"
        android:textSize="28sp"
        android:textStyle="bold"
        android:layout_marginBottom="16dp"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Your support keeps DREs Quiz alive and growing. Every contribution — big or small — powers this mission and means more than words can say."
        android:textColor="@color/white_alpha"
        android:textSize="16sp"
        android:gravity="center"
        android:lineSpacingMultiplier="1.4"
        android:layout_marginBottom="40dp"/>

    <com.google.android.material.button.MaterialButton
            android:id="@+id/btnPayPal"
            android:layout_width="260dp"
            android:layout_height="60dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="16dp"
            app:backgroundTint="#1A1F3D"
            app:strokeColor="#FFD700"
            app:strokeWidth="1.5dp"
            app:cornerRadius="12dp"
            app:rippleColor="#66FFD700"
            android:stateListAnimator="@null"
            android:elevation="0dp"
            android:text="💳 DONATE VIA PAYPAL"
            android:textColor="@color/gold"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textAllCaps="false"
            android:letterSpacing="0.06"
            android:insetTop="0dp"
            android:insetBottom="0dp"/>

    <com.google.android.material.button.MaterialButton
            android:id="@+id/btnBackDonate"
            android:layout_width="260dp"
            android:layout_height="60dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="24dp"
            app:backgroundTint="#1A1F3D"
            app:strokeColor="#FFD700"
            app:strokeWidth="1.5dp"
            app:cornerRadius="12dp"
            app:rippleColor="#66FFD700"
            android:stateListAnimator="@null"
            android:elevation="0dp"
            android:text="← BACK"
            android:textColor="@color/gold"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textAllCaps="false"
            android:letterSpacing="0.06"
            android:insetTop="0dp"
            android:insetBottom="0dp"/>
</LinearLayout>
